;GCD of two numbers
	AREA REST, CODE
	ENTRY

	LDR R0, =NUM
	LDR R1, [R0]
	LDR R2, [R0,#4]

LOOP 
	CMP R1, R2
	BEQ EXIT
	SUBLT R2, R1
	SUBGT R1, R2
	B LOOP

EXIT
	LDR R0, =RES
	STR R1, [R0]

STOP B STOP

NUM DCD 0X1E, 0x2D

	AREA RESULT, DATA
RES SPACE 4
	END