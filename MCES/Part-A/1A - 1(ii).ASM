	AREA RESET, CODE
	ENTRY
	; A = 2 * A + B
	
	MOV R0, #NUM1	; A (2)
	MOV R1, #NUM2	; B (3)		
	ADD R0, R1, R0, LSL #1	; LEFT SHIFT 1 BIT TO MULTIPLY BY 2 (OPERAND, LSL #NO_OF_BITS)
	
STOP B STOP
NUM1 EQU 2
NUM2 EQU 3
	END